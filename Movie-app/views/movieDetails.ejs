<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title><%= record.title %> - Movie Details</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background: #f2f2f2;
      font-family: Arial, sans-serif;
    }
    .movie-poster {
      max-width: 100%;
      border-radius: 10px;
      box-shadow: 0 4px 8px rgba(0,0,0,0.2);
    }
    .section-title {
      border-bottom: 2px solid #ccc;
      margin-top: 30px;
      margin-bottom: 15px;
      font-weight: bold;
      font-size: 1.25rem;
    }
    .info-box {
      background: #fff;
      padding: 20px;
      border-radius: 8px;
      margin-bottom: 20px;
      box-shadow: 0 2px 6px rgba(0,0,0,0.1);
    }
  </style>
</head>
<body class="p-4">

  <div class="container">
    <!-- Title and Poster -->
    <div class="row mb-4">
      <div class="col-md-3">
        <% if (record.posterimg) { %>
          <img src="<%= record.posterimg %>" alt="<%= record.title %> Poster" class="movie-poster">
        <% } else { %>
          <p>No poster image available</p>
        <% } %>
      </div>
      <div class="col-md-9">
        <h1 class="display-5"><%= record.title %></h1>
        <p><strong>Rating:</strong> <%= record.rating %></p>
        <p><strong>Age Restriction:</strong> <%= record.age %></p>
        <p><strong>Duration:</strong> <%= record.duration %> min</p>
        <p><strong>Language:</strong>
          <% if (record.language && record.language.length > 0) { %>
            <%= record.language.join(', ') %>
          <% } else { %>
            Not specified
          <% } %>
        </p>
        <p><strong>Genre:</strong>
          <% if (record.genre && record.genre.length > 0) { %>
            <%= record.genre.join(', ') %>
          <% } else { %>
            Not specified
          <% } %>
        </p>
      </div>
    </div>

    <div class="info-box">
      <div class="section-title">Description</div>
      <p><%= record.desc %></p>
    </div>

    <div class="info-box">
      <div class="section-title">Cast</div>
      <p><%= record.cast %></p>
    </div>

    

</body>
</html>
